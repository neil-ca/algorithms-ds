package tdd;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class PlayerTest {
    @Test public void loses_when_dice_number_is_low() {
        assertEquals(false, true);
    }
}
